---
// Route file only. The page itself lives in src/views/ContactPage.astro,
// shared by all three languages; this fixes the URL and the locale.
import ContactPage from "../../../views/ContactPage.astro";
---

<ContactPage locale="ca" />
