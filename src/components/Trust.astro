---
import { fill, t, type Locale } from "../i18n";

interface Props { locale: Locale }
const { locale } = Astro.props;
const d = t(locale);
---

<section class="border-b border-paper-edge bg-white py-16 md:py-24">
  <div class="wrap grid gap-10 lg:grid-cols-[0.85fr_1.15fr] lg:gap-16">
    <div>
      <p class="label">{d.trust.label}</p>
      <h2 class="mt-3 text-[clamp(1.875rem,4.5vw,2.75rem)]">{d.trust.title}</h2>
      <p class="mt-4 text-lg leading-relaxed text-ink-70">{d.trust.lead}</p>
    </div>

    <ul class="grid gap-px self-start bg-paper-edge sm:grid-cols-2">
      {
        d.trust.points.map((point) => (
          <li class="bg-white p-6 sm:last:col-span-2">
            <h3 class="font-display text-base font-bold leading-tight">{fill(point.title)}</h3>
            <p class="mt-2.5 text-sm leading-relaxed text-ink-70">{fill(point.body)}</p>
          </li>
        ))
      }
    </ul>
  </div>
</section>
